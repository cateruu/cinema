import React from 'react';
import { Schedule } from '../../../../types/schedule';

interface Props {
  schedule: Schedule;
}

const ScheduleElement = ({ schedule }: Props) => {
  return <div>ScheduleElement</div>;
};

export default ScheduleElement;
